# AI Finance Assistant
HSE VIBE HACK команда SHIFT HAPPENS x MISIS

Финансовый ассистент с распознаванием голоса и анализом трат на базе GigaChat.

## Возможности

- 🎤 Распознавание речи (speech-to-text)
- 📊 Анализ трат через GigaChat AI
- 💰 Автоматическое извлечение структурированных данных о тратах
- 📈 Аналитика по категориям и рекомендации

## Структура проекта

- `backend/` - FastAPI сервер с интеграцией GigaChat
- `frontend/` - .NET MAUI приложение

## Установка и запуск

### Backend

1. Установите зависимости:
```bash
cd backend
pip install -r requirements.txt
```

2. Запустите сервер:
```bash
python main.py
```

Сервер будет доступен по адресу `http://localhost:8000`

### Frontend

1. Откройте решение в Visual Studio или Rider
2. Убедитесь, что в `MainPage.xaml.cs` указан правильный `API_BASE_URL`
3. Запустите приложение на выбранной платформе

## Использование

1. Введите текст о ваших тратах в текстовое поле или выберите аудио файл
2. Нажмите "Анализировать" или "Голосовой ввод"
3. Получите структурированный анализ трат с рекомендациями

## API Endpoints

- `POST /api/analyze-expenses` - анализ текста о тратах
- `POST /api/analyze-audio-expenses` - анализ аудио файла
- `POST /api/transcribe` - распознавание речи
- `GET /api/health` - проверка здоровья сервера
