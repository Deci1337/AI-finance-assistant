# HSE Finance Assistant

AI-ассистент для управления личными финансами с интеллектуальным анализом расходов и прогнозированием.

## О проекте

HSE Finance Assistant — это кроссплатформенное мобильное приложение для учета и анализа личных финансов с интеграцией AI-модели GigaChat от Сбербанка. Приложение помогает пользователям отслеживать расходы, анализировать финансовые привычки, получать персональные рекомендации и прогнозировать будущие траты.

### Основные возможности

- 📊 Учет доходов и расходов с категоризацией
- 🤖 AI-чат для финансовых консультаций
- 📈 Анализ расходов по категориям и месяцам
- 💡 Прогнозирование и симуляция будущих расходов
- 🎯 Рекомендации по оптимизации бюджета
- 😊 Анализ эмоционального тона сообщений
- 🎤 Голосовой ввод транзакций
- 📱 Кроссплатформенность (Android, iOS, Windows, macOS)

## Технологии

### Backend

- **Python 3.x** — основной язык разработки
- **FastAPI** — современный веб-фреймворк для создания REST API
- **Uvicorn** — ASGI-сервер для запуска приложения
- **Pydantic** — валидация данных и настройки
- **GigaChat API** — нейросетевая модель от Сбербанка для:
  - Анализа эмоций в тексте
  - Генерации финансовых советов
  - Извлечения транзакций из текста
  - Чат-ассистента для финансовых вопросов

### Frontend

- **.NET MAUI (Multi-platform App UI)** — кроссплатформенный фреймворк
- **C#** — язык разработки
- **XAML** — разметка пользовательского интерфейса
- **SQLite** — локальная база данных для хранения транзакций
- **Plugin.Maui.Audio** — работа с аудио для голосового ввода

### Архитектура

Проект построен по архитектуре клиент-сервер:

```
┌─────────────────┐         HTTP/REST API         ┌─────────────────┐
│                 │ ◄───────────────────────────► │                 │
│  MAUI Frontend  │                                │  FastAPI Backend│
│   (Mobile App)  │                                │   (Python API)  │
│                 │                                │                 │
└─────────────────┘                                └────────┬────────┘
                                                            │
                                                            ▼
                                                   ┌─────────────────┐
                                                   │   GigaChat API  │
                                                   │  (AI Model)     │
                                                   └─────────────────┘
```

**Backend структура:**
- `app/api/routes/` — REST API endpoints (chat, transactions, emotions, portfolio, voice, health)
- `app/services/` — бизнес-логика (AI сервис, аналитика, портфель)
- `app/models/` — модели данных (Pydantic схемы)
- `app/core/` — конфигурация и middleware
- `gigachat_integration.py` — интеграция с GigaChat API

**Frontend структура:**
- `Pages/` — страницы приложения (MainPage, StatisticsPage, AIAssistantPage и др.)
- `Services/` — сервисы для работы с API и локальной БД
- `Models/` — модели данных
- `Controls/` — кастомные элементы управления (графики)

## Нейросеть

Проект использует **GigaChat** — языковую модель от Сбербанка для:

1. **Анализ эмоций** — определение эмоционального состояния пользователя (радость, страх, злость, грусть, удивление, нейтральность)
2. **Анализ дружелюбности** — оценка тона сообщений с учетом эмоциональных весов
3. **Финансовый чат-ассистент** — ответы на вопросы о финансах, анализ расходов, прогнозирование
4. **Извлечение транзакций** — парсинг финансовых данных из текста пользователя
5. **Генерация инсайтов** — персональные рекомендации на основе истории расходов

## Установка и запуск

### Требования

**Backend:**
- Python 3.8+
- pip

**Frontend:**
- .NET 9.0 SDK
- Visual Studio 2022 или Visual Studio Code с расширением C# Dev Kit
- Для Android: Android SDK
- Для iOS: macOS с Xcode (только для разработки под iOS)

### Backend

1. Перейдите в директорию backend:
```bash
cd backend
```

2. Создайте виртуальное окружение (рекомендуется):
```bash
# Windows
python -m venv venv
venv\Scripts\activate

# Windows (PowerShell) - если требуется разрешение выполнения скриптов:
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope Process
.\venv\Scripts\Activate.ps1

# Linux/macOS
python3 -m venv venv
source venv/bin/activate
```

3. Установите зависимости:
```bash
pip install -r requirements.txt
```

4. Настройте переменные окружения (опционально):
```bash
# Windows PowerShell
$env:GIGACHAT_CLIENT_ID="your_client_id"
$env:GIGACHAT_CLIENT_SECRET="your_client_secret"
$env:HOST="0.0.0.0"
$env:PORT="8000"

# Linux/macOS
export GIGACHAT_CLIENT_ID="your_client_id"
export GIGACHAT_CLIENT_SECRET="your_client_secret"
export HOST="0.0.0.0"
export PORT="8000"
```

**Примечание:** По умолчанию используются тестовые учетные данные GigaChat. Для продакшена замените их на свои.

5. Запустите сервер:

**Windows:**
```bash
python main.py
```

**Linux/macOS:**
```bash
# Используя скрипт
chmod +x start_server.sh
./start_server.sh

# Или напрямую
python3 main.py
```

Сервер будет доступен по адресу: `http://localhost:8000`

API документация (Swagger UI): `http://localhost:8000/docs`

### Frontend

1. Перейдите в директорию frontend:
```bash
cd frontend/FinanceAssistant
```

2. Восстановите NuGet пакеты:
```bash
dotnet restore
```

3. Запустите приложение:

**Windows:**
```bash
dotnet build
dotnet run --project FinanceAssistant/FinanceAssistant.csproj
```

**Android (требуется Android SDK):**
```bash
dotnet build -t:Run -f net9.0-android
```

**iOS (только на macOS):**
```bash
dotnet build -t:Run -f net9.0-ios
```

**Через Visual Studio:**
- Откройте `FinanceAssistant.sln`
- Выберите целевую платформу (Windows, Android, iOS)
- Нажмите F5 для запуска

### Настройка подключения к Backend

В приложении настройте URL backend сервера в настройках или через `Services/ApiService.cs`. По умолчанию используется `http://localhost:8000`.

Для Android эмулятора используйте `http://10.0.2.2:8000`.

## API Endpoints

- `GET /` — проверка здоровья сервера
- `GET /health` — проверка здоровья (legacy)
- `POST /api/chat` — чат с AI-ассистентом
- `POST /api/emotions/analyze` — анализ эмоций в тексте
- `POST /api/emotions/friendliness` — анализ дружелюбности
- `POST /api/transactions/extract` — извлечение транзакций из текста
- `POST /api/portfolio/analyze` — анализ портфеля
- `POST /api/voice/transcribe` — транскрипция аудио

Полная документация доступна по адресу `/docs` после запуска сервера.

## Структура проекта

```
HSE finance/
├── backend/                 # Backend API (FastAPI)
│   ├── app/
│   │   ├── api/           # API routes
│   │   ├── core/          # Конфигурация и middleware
│   │   ├── models/        # Pydantic модели
│   │   └── services/      # Бизнес-логика
│   ├── gigachat_integration.py
│   ├── main.py            # Точка входа
│   └── requirements.txt   # Python зависимости
│
└── frontend/              # Frontend приложение (MAUI)
    └── FinanceAssistant/
        └── FinanceAssistant/
            ├── Pages/     # Страницы приложения
            ├── Services/  # Сервисы (API, БД)
            ├── Models/    # Модели данных
            └── Controls/  # Кастомные контролы
```

## Разработка

### Backend разработка

Сервер автоматически перезагружается при изменении кода в режиме разработки (кроме Termux/Android).

Для отладки используйте:
```bash
uvicorn main:app --reload --host 0.0.0.0 --port 8000
```

### Frontend разработка

Для горячей перезагрузки используйте Visual Studio или запускайте с флагом `--watch`:
```bash
dotnet watch run
```

## Лицензия

Проект разработан в рамках учебного курса HSE.

## Контакты

Для вопросов и предложений создайте Issue в репозитории.
