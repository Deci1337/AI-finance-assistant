# Инструкция по тестированию голосового ввода

## Подготовка к тестированию

### 1. Запуск Backend

```bash
cd backend
python main.py
```

Backend должен быть доступен по адресу `http://localhost:8000`

### 2. Проверка зависимостей

Убедитесь, что установлены все зависимости:

```bash
pip install -r backend/requirements.txt
```

Особенно важны:
- `fastapi`
- `python-multipart` (для загрузки файлов)
- `requests`

## Способы тестирования

### Способ 1: Тестирование через приложение (MAUI)

1. **Запустите MAUI приложение** на эмуляторе или реальном устройстве
2. **Откройте чат** (нажмите на кнопку "AI" в нижней панели)
3. **Нажмите на кнопку микрофона** 🎤
4. **Разрешите доступ к микрофону** (при первом использовании)
5. **Произнесите фразу**, например:
   - "Купил хлеб за 50 рублей и молоко за 80 рублей"
   - "Получил зарплату 85000 рублей"
   - "Вчера потратил 2000 на обед в ресторане"
6. **Остановите запись** (нажмите на кнопку микрофона еще раз или закройте диалог записи)
7. **Проверьте результат**:
   - Должен появиться распознанный текст
   - Должны быть извлечены транзакции
   - Должна быть возможность добавить транзакции

### Способ 2: Тестирование через API (Python скрипт)

1. **Запустите тестовый скрипт**:

```bash
cd backend
python test_voice_api.py
```

2. **Следуйте инструкциям** в скрипте

3. **Для тестирования с аудио файлом**:
   - Подготовьте WAV файл с записью голоса
   - Укажите путь к файлу при запросе скрипта

### Способ 3: Тестирование через curl

#### Тест распознавания речи:

```bash
curl -X POST "http://localhost:8000/transcribe-voice" \
  -F "audio=@path/to/your/audio.wav" \
  -F "audio_format=wav"
```

#### Тест распознавания и извлечения транзакций:

```bash
curl -X POST "http://localhost:8000/transcribe-and-extract" \
  -F "audio=@path/to/your/audio.wav" \
  -F "audio_format=wav"
```

### Способ 4: Тестирование через Postman

1. **Создайте новый POST запрос**:
   - URL: `http://localhost:8000/transcribe-and-extract`
   - Method: POST

2. **В Body выберите form-data**:
   - Добавьте поле `audio` типа File
   - Выберите ваш аудио файл
   - Добавьте поле `audio_format` типа Text со значением `wav`

3. **Отправьте запрос** и проверьте ответ

## Проверка работы компонентов

### 1. Проверка доступности API

```bash
curl http://localhost:8000/health
```

Должен вернуться статус `200 OK` с JSON ответом.

### 2. Проверка распознавания речи

Эндпоинт `/transcribe-voice` должен:
- Принимать аудио файл
- Возвращать распознанный текст
- Обрабатывать ошибки корректно

### 3. Проверка извлечения транзакций

Эндпоинт `/transcribe-and-extract` должен:
- Распознавать речь из аудио
- Извлекать транзакции из распознанного текста
- Возвращать структурированные данные

### 4. Проверка интеграции с GigaChat

Убедитесь, что:
- GigaChat API доступен
- Токен доступа получается корректно
- Распознавание речи работает через GigaChat/SaluteSpeech

## Типичные проблемы и решения

### Проблема: "Не удалось распознать речь"

**Возможные причины:**
1. Аудио файл поврежден или в неподдерживаемом формате
2. GigaChat API недоступен
3. Плохое качество записи

**Решения:**
- Используйте формат WAV с частотой дискретизации 16kHz
- Проверьте подключение к интернету
- Убедитесь, что GigaChat API доступен

### Проблема: "Ошибка при загрузке файла"

**Возможные причины:**
1. Файл слишком большой
2. Неправильный формат файла
3. Проблемы с CORS

**Решения:**
- Используйте файлы размером до 10MB
- Убедитесь, что формат поддерживается (wav, mp3, oggopus)
- Проверьте настройки CORS в backend

### Проблема: "Разрешение на микрофон не предоставлено"

**Решение:**
- В настройках устройства разрешите доступ к микрофону для приложения
- Перезапустите приложение после предоставления разрешения

## Тестовые фразы для проверки

Используйте следующие фразы для тестирования:

1. **Простая транзакция:**
   - "Купил хлеб за 50 рублей"

2. **Множественные транзакции:**
   - "Купил хлеб за 50 рублей и молоко за 80 рублей"

3. **Доход:**
   - "Получил зарплату 85000 рублей"

4. **С указанием даты:**
   - "Вчера потратил 2000 на обед в ресторане"

5. **Сложная фраза:**
   - "На прошлой неделе я потратил 5000 рублей на продукты, 2000 на транспорт и получил зарплату 85000 рублей"

## Проверка логов

### Backend логи

Проверьте консоль backend для отладки:
- Ошибки подключения к GigaChat API
- Проблемы с распознаванием речи
- Ошибки обработки файлов

### Frontend логи

В MAUI приложении проверьте:
- Ошибки записи аудио
- Проблемы с отправкой запросов
- Ошибки десериализации ответов

## Автоматическое тестирование

Для автоматического тестирования используйте `test_voice_api.py`:

```bash
python backend/test_voice_api.py
```

Скрипт проверит:
- Доступность API
- Распознавание речи (если предоставлен аудио файл)
- Извлечение транзакций из текста

## Дополнительные ресурсы

- [Документация GigaChat API](https://developers.sber.ru/docs/ru/gigachat/api/reference)
- [Документация SaluteSpeech](https://developers.sber.ru/docs/ru/salutespeech/api/reference)
- [FastAPI документация](https://fastapi.tiangolo.com/)

