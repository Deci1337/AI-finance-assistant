# Работа на Android без дополнительного ПО

## Варианты работы без Termux

### Вариант 1: Облачный сервер (Рекомендуется) ⭐

**Самый простой и надежный вариант - не требует установки ничего на телефон.**

#### Настройка на Render.com (бесплатно):

1. Зарегистрируйтесь на [render.com](https://render.com)
2. Подключите GitHub репозиторий
3. Создайте новый **Web Service**
4. Настройки:
   - **Build Command**: `cd backend && pip install -r requirements.txt`
   - **Start Command**: `cd backend && python main.py`
   - **Environment**: Python 3
5. Получите URL (например: `https://your-app.onrender.com`)
6. В приложении настройте этот URL

#### Настройка в приложении:

В `FinanceService.cs` или через настройки приложения установите:
```csharp
Preferences.Set("api_base_url", "https://your-app.onrender.com");
```

**Преимущества:**
- ✅ Не требует установки ПО на телефон
- ✅ Работает везде где есть интернет
- ✅ Автоматический запуск при перезагрузке
- ✅ Бесплатный тариф доступен

---

### Вариант 2: Локальный сервер на ПК в той же Wi-Fi сети

**Запускайте backend на компьютере, телефон подключается по Wi-Fi.**

#### Настройка:

1. **На компьютере:**
   ```bash
   cd backend
   python main.py
   ```
   Сервер запустится на `http://0.0.0.0:8000`

2. **Узнайте IP адрес компьютера:**
   - Windows: `ipconfig` (найдите IPv4 адрес в разделе Wi-Fi)
   - Mac/Linux: `ifconfig` или `ip addr`
   - Обычно это `192.168.1.XXX` или `192.168.0.XXX`

3. **В приложении на телефоне:**
   - Откройте настройки приложения
   - Введите IP адрес: `http://192.168.1.XXX:8000`
   - Или используйте автоматический поиск (приложение попробует найти сервер)

#### Автоматический поиск сервера:

Приложение автоматически попробует найти backend в локальной сети при запуске.

**Преимущества:**
- ✅ Не требует установки ПО на телефон
- ✅ Работает без интернета (только Wi-Fi)
- ✅ Быстро и удобно для разработки

**Недостатки:**
- ⚠️ Компьютер должен быть включен
- ⚠️ Оба устройства должны быть в одной Wi-Fi сети

---

### Вариант 3: Встроенный Python в приложение (Сложный)

Можно встроить Python runtime прямо в Android приложение используя:
- **Chaquopy** (плагин для Android)
- **BeeWare** (Python для мобильных)

Это требует переработки архитектуры и увеличивает размер приложения.

---

## Рекомендация

**Для продакшена:** Используйте **Вариант 1** (облачный сервер)

**Для разработки:** Используйте **Вариант 2** (локальный сервер на ПК)

## Настройка URL в приложении

### Способ 1: Через код

В `FinanceService.cs` измените:
```csharp
private const string API_BASE_URL = "https://your-cloud-server.com";
```

### Способ 2: Через настройки приложения

Добавьте экран настроек где пользователь может ввести URL сервера:
```csharp
var url = await DisplayPromptAsync("Настройка сервера", "Введите URL backend сервера:");
if (!string.IsNullOrEmpty(url))
{
    Preferences.Set("api_base_url", url);
}
```

### Способ 3: Автоматический поиск

Приложение автоматически попробует найти сервер в локальной сети при первом запуске.

## Проверка подключения

После настройки проверьте:
```bash
# На телефоне или через curl
curl http://YOUR_SERVER_URL/health
```

## Итог

**Для работы на Android БЕЗ дополнительного ПО:**
1. Используйте облачный сервер (Render.com, Railway.app и т.д.)
2. Или запускайте backend на ПК и подключайтесь по Wi-Fi
3. Приложение автоматически найдет сервер в локальной сети если он доступен

**Никаких дополнительных установок на телефон не требуется!** 🎉

