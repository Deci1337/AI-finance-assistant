# ML-компоненты AI Finance Assistant

## 📋 Обзор

Приложение использует комплексный подход к машинному обучению, сочетая:
- **Large Language Models (LLM)** через GigaChat API для понимания естественного языка
- **Speech Recognition** через Sber Smart Speech для распознавания голоса
- **Rule-based extraction** для извлечения структурированных данных
- **Fallback механизмы** для обеспечения надежности

---

## 🧠 Основные ML-компоненты

### 1. **GigaChat Integration** - Ядро AI системы

**Технология:** GigaChat (российская LLM от Сбера)

**Модели:**
- `GigaChat` - базовая модель (основная, экономия токенов)
- `GigaChat-Plus` - расширенная версия (fallback)
- `GigaChat-Pro` - профессиональная версия (fallback)

**Автоматический выбор модели:**
```python
def detect_available_model() -> str:
    # Пробует модели в порядке приоритета
    # Кэширует результат для оптимизации
    # Использует первую доступную модель
```

**Особенности:**
- Автоматическое определение доступной модели
- Кэширование токенов доступа
- Fallback на более простые модели при ошибках
- Оптимизация использования токенов

---

### 2. **Анализ эмоций (Emotion Analysis)**

**Задача:** Определение эмоционального состояния пользователя по тексту

**Метод:** Fine-tuned промпты для GigaChat с извлечением параметров

**Выходные данные:**
- Распределение эмоций (joy, fear, anger, sadness, surprise, neutral) - значения 0.0-1.0
- Детальный анализ эмоционального состояния
- Уточняющие вопросы
- Рекомендации по управлению эмоциями

**Промпт-инжиниринг:**
```python
prompt = f"""Ты опытный психолог и финансовый консультант. 
Проанализируй эмоциональное состояние пользователя на основе его текста, 
учитывая извлеченные параметры:
- Финансовые цели и приоритеты
- Упоминаемые суммы и временные рамки
- Уровень опыта и знаний
- Текущую финансовую ситуацию
"""
```

**Fallback:** Простой анализ на основе ключевых слов (regex patterns)

**Использование:**
- Учет эмоций при финансовых рекомендациях
- Персонализация тона общения
- Предотвращение импульсивных решений

---

### 3. **Извлечение транзакций (Transaction Extraction)**

**Задача:** Автоматическое извлечение структурированных данных о транзакциях из естественного языка

**Метод:** Комбинация rule-based extraction + LLM

**Извлекаемые параметры:**
- Тип транзакции (доход/расход)
- Сумма (с конвертацией валют)
- Категория (Food, Transport, Entertainment, Health, Shopping, etc.)
- Дата (с обработкой относительных дат: "вчера", "неделю назад")
- Описание/название транзакции
- Уровень уверенности (confidence score 0.0-1.0)

**Процесс:**

1. **Предварительное извлечение параметров** (rule-based):
```python
def _extract_parameters_from_message(self, message: str) -> Dict:
    # Извлечение сумм через regex patterns
    # Определение временных рамок
    # Определение категорий
    # Определение целей
    # Определение уровня риска
    # Определение эмоционального состояния
```

2. **LLM обработка** через GigaChat:
```python
prompt = f"""Ты опытный финансовый аналитик. 
Извлеки из сообщения ВСЕ транзакции:
- Определи тип (доход/расход)
- Извлеки сумму (конвертируй в рубли)
- Определи категорию
- Определи дату
- Извлеки описание
"""
```

3. **Адаптивный анализ сложности:**
- Простые сообщения (1 транзакция) → краткий анализ (1-3 предложения)
- Средние сообщения (2-3 транзакции) → средний анализ (3-5 предложений)
- Сложные сообщения (много транзакций) → развернутый анализ (8-10+ предложений)

**Fallback:** Простое извлечение через regex и шаблоны

**Примеры:**
- "Потратил 500 рублей на обед в кафе" → 1 транзакция, категория Food
- "Вчера купил продукты на 2000, сегодня заправил машину на 3000" → 2 транзакции
- "За последнюю неделю потратил на развлечения 5000, на еду 3000, получил зарплату 100000" → 3 транзакции

---

### 4. **Распознавание речи (Speech Recognition)**

**Технология:** Sber Smart Speech API + GigaChat Audio API

**Поддерживаемые форматы:**
- PCM16 (основной)
- WAV, MP3, OGG Opus, Opus, FLAC, ALAW, MULAW

**Архитектура:**

1. **GigaChat Audio API** (первичный метод):
```python
def transcribe_audio_gigachat(access_token, audio_data, audio_format):
    # Отправка аудио в base64 формате
    # Использование GigaChat для распознавания
```

2. **Sber Smart Speech API** (fallback):
```python
def transcribe_audio_salute_speech(access_token, audio_data, audio_format):
    # Прямая отправка аудио в Smart Speech
    # Обработка потокового ответа
```

3. **Комбинированный подход:**
```python
def transcribe_audio_with_fallback(audio_data, audio_format):
    # Пробует GigaChat Audio API
    # При ошибке → Smart Speech API
    # Возвращает распознанный текст
```

**Особенности:**
- Автоматический fallback между методами
- Поддержка различных аудио форматов
- Обработка ошибок и таймаутов

---

### 5. **Комплексный финансовый анализ (Comprehensive Analysis)**

**Задача:** Многофакторный анализ финансовой ситуации с учетом всех параметров

**Извлекаемые параметры:**
- Суммы денег
- Временные рамки (краткосрочные/среднесрочные/долгосрочные)
- Финансовые цели (накопление, инвестиции, покупка, погашение долга)
- Толерантность к риску (консервативный/умеренный/агрессивный)
- Категории трат/инвестиций
- Приоритеты и ограничения
- Уровень финансового опыта
- Эмоциональное состояние
- Текущая финансовая ситуация

**Выходные данные:**
- Комплексный анализ ситуации (4-5 предложений)
- Анализ финансовых целей
- Оценка рисков
- Эмоциональный анализ
- Персонализированные рекомендации (с приоритетами)
- Финансовый план с конкретными шагами
- Уточняющие вопросы
- Предупреждения и соображения

**Промпт-структура:**
```
ИНСТРУКЦИИ ДЛЯ АНАЛИЗА:
1. ФИНАНСОВЫЕ ЦЕЛИ
2. ТОЛЕРАНТНОСТЬ К РИСКУ
3. ЭМОЦИОНАЛЬНОЕ СОСТОЯНИЕ
4. ФИНАНСОВАЯ СИТУАЦИЯ
5. ОПЫТ И ЗНАНИЯ
6. КАТЕГОРИИ И ПРИОРИТЕТЫ
7. СУММЫ И РЕСУРСЫ
```

**Пример использования:**
Вход: "Хочу накопить 50 тысяч за полгода, но трачу много на доставку еды"

Выход:
- Извлеченные параметры: цель (50к), срок (6 мес), проблема (доставка)
- Анализ: оценка текущей ситуации
- Рекомендации: конкретный план действий
- Финансовый план: пошаговые действия с суммами

---

### 6. **Генерация прогнозов (Forecast Generation)**

**Задача:** Прогнозирование финансовых сценариев на основе изменений в категориях

**Метод:** Статистический анализ + LLM для интерпретации

**Процесс:**

1. **Анализ текущих расходов:**
```python
# Группировка транзакций по категориям
# Вычисление средних месячных расходов
# Определение временных паттернов
```

2. **Извлечение параметров прогноза через LLM:**
```python
prompt = f"""Проанализируй запрос пользователя и извлеки:
- Какие категории изменить
- На сколько процентов изменить
- Количество месяцев для прогноза
"""
```

3. **Генерация прогноза:**
```python
# Расчет новых месячных расходов
# Генерация прогноза по месяцам
# Расчет общей экономии/увеличения расходов
# Генерация текстового описания через LLM
```

**Выходные данные:**
- Категория для прогноза
- Текущие средние месячные расходы
- Процент изменения
- Новые средние месячные расходы
- Прогноз по месяцам (список с деталями по каждому месяцу)
- Общая экономия/увеличение за период
- Текстовое описание прогноза

**Пример:**
Вход: "Что будет, если я уменьшу траты на развлечения на 25%?"

Выход:
- Категория: Entertainment
- Текущие расходы: 10000₽/месяц
- Изменение: -25%
- Новые расходы: 7500₽/месяц
- Прогноз на 3 месяца: экономия 7500₽
- Описание: "При уменьшении расходов на развлечения на 25% вы сможете сэкономить..."

**Fallback:** Простой прогноз на основе regex парсинга запроса

---

### 7. **Генерация инсайтов (Insights Generation)**

**Задача:** Автоматическое выявление паттернов в транзакциях и генерация персональных рекомендаций

**Метод:** Статистический анализ паттернов + LLM для формулировки

**Процесс:**

1. **Анализ транзакций:**
```python
# Группировка по категориям
# Подсчет транзакций с низкой важностью
# Выявление повторяющихся паттернов
# Сравнение с предыдущими периодами
```

2. **Генерация инсайта через LLM:**
```python
prompt = f"""Проанализируй паттерн покупок:
- Категория: {category}
- Количество покупок: {count}
- Необязательные покупки: {low_importance_count}
- Общая сумма: {total}₽

Создай краткий инсайт (2-3 предложения), который:
1. Обращает внимание на паттерн
2. Приводит конкретные цифры
3. Дает практическую рекомендацию
"""
```

**Выходные данные:**
- Текстовый инсайт (2-3 предложения)
- Категория
- Сумма трат
- Количество транзакций
- Количество необязательных покупок
- Процент необязательных покупок
- Сравнение с предыдущим месяцем
- Процент от общих расходов

**Пример:**
Инсайт: "Заметил, что вы несколько раз оценивали Food как необязательную покупку. В этом месяце на Food ушло 15000₽. Возможно, стоит попробовать альтернативный продукт или покупать реже?"

**Fallback:** Простой инсайт на основе статистики без LLM

---

### 8. **Анализ дружелюбности (Friendliness Analysis)**

**Задача:** Определение тона и дружелюбности ответов AI

**Метод:** LLM анализ сентимента

**Выходные данные:**
- Friendliness Score (0.0-1.0)
- Sentiment (positive/neutral/negative)
- Timestamp

**Использование:** Для контроля качества ответов AI и адаптации тона общения

---

### 9. **Извлечение параметров из сообщения (Parameter Extraction)**

**Задача:** Автоматическое извлечение структурированных параметров из неструктурированного текста

**Метод:** Rule-based extraction через regex patterns и ключевые слова

**Извлекаемые параметры:**

1. **Суммы:**
```python
amount_patterns = [
    r'(\d+[\s,.]?\d*)\s*(рубл[ейя]|rub|₽|р\.|руб)',
    r'(\d+[\s,.]?\d*)\s*(тысяч|тыс|к|k)',
    r'(\d+[\s,.]?\d*)\s*(миллион|млн|m)',
    r'(\d+[\s,.]?\d*)\s*(доллар|usd|\$|бакс)',
    r'(\d+[\s,.]?\d*)\s*(евро|eur|€)'
]
```

2. **Временные рамки:**
- Краткосрочные: месяц, неделя, день, скоро, быстро
- Среднесрочные: полгода, год, несколько месяцев
- Долгосрочные: годы, несколько лет, 5 лет, 10 лет

3. **Финансовые цели:**
- Накопление, инвестиции, покупка, погашение долга, пенсия, образование, недвижимость

4. **Толерантность к риску:**
- Консервативный, умеренный, агрессивный

5. **Категории:**
- Еда, транспорт, развлечения, здоровье, образование, жилье, акции, облигации

6. **Приоритеты:**
- Высокий, средний, низкий

7. **Ограничения:**
- Ограниченный бюджет, временные ограничения, правовые ограничения

8. **Уровень опыта:**
- Новичок, опытный, эксперт

9. **Эмоциональное состояние:**
- Тревога, радость, грусть, злость

10. **Финансовая ситуация:**
- Стабильная, критическая, растущая

**Использование:** Все извлеченные параметры используются для персонализации ответов AI

---

## 🔄 Архитектура Fallback механизмов

Все ML-компоненты имеют fallback механизмы для обеспечения надежности:

```
Основной метод (GigaChat API)
    ↓ (при ошибке)
Fallback метод (rule-based / простой анализ)
    ↓ (при ошибке)
Минимальный ответ / предупреждение пользователю
```

**Преимущества:**
- Приложение работает даже при недоступности API
- Постепенная деградация функциональности
- Пользователь всегда получает ответ

---

## 📊 Параметры LLM запросов

### Temperature (температура генерации):
- **0.3** - для точных задач (извлечение данных, прогнозы)
- **0.7** - для творческих задач (анализ, рекомендации)
- **0.8** - для эмоционального анализа

### Max Tokens:
- **200-300** - для простых ответов (извлечение параметров)
- **2000** - для стандартных ответов (анализ, рекомендации)
- **4000** - для комплексного анализа

---

## 🎯 Оптимизация и производительность

### Кэширование:
- Токены доступа к GigaChat API
- Результаты определения доступной модели
- Параметры извлечения (для повторных запросов)

### Оптимизация токенов:
- Использование базовой модели GigaChat по умолчанию
- Переход на Pro только при необходимости
- Адаптивная длина промптов в зависимости от сложности задачи

### Обработка ошибок:
- Таймауты для всех API запросов (15-30 секунд)
- Retry логика для временных ошибок
- Graceful degradation при недоступности API

---

## 🔐 Безопасность и конфиденциальность

- Все финансовые данные хранятся локально
- В API отправляются только необходимые данные
- Токены доступа кэшируются локально
- SSL/TLS для всех API запросов
- Валидация входных данных перед отправкой в API

---

## 📈 Метрики качества

### Для извлечения транзакций:
- Confidence score (0.0-1.0) для каждой транзакции
- Валидация извлеченных данных
- Обработка неоднозначностей

### Для анализа эмоций:
- Нормализация распределения эмоций (сумма ≈ 1.0)
- Валидация диапазонов значений

### Для прогнозов:
- Статистическая валидация данных
- Проверка на достаточность данных (минимум 3 транзакции)

---

## 🚀 Будущие улучшения

1. **Fine-tuning моделей** на финансовых данных
2. **Векторные базы данных** для контекстного поиска
3. **Ensemble методы** для повышения точности
4. **A/B тестирование** промптов
5. **Метрики качества** в реальном времени
6. **Персонализация моделей** под пользователя
7. **Офлайн режим** с локальными моделями

---

## 📝 Примеры использования

### Пример 1: Голосовой ввод транзакции
```
Пользователь: [голос] "Потратил 500 рублей на обед"
    ↓
Speech Recognition → "Потратил 500 рублей на обед"
    ↓
Transaction Extraction → {
    type: "expense",
    amount: 500,
    category: "Food",
    date: "2024-01-15"
}
```

### Пример 2: Комплексный анализ
```
Пользователь: "Хочу накопить 50 тысяч за полгода, но трачу много на доставку"
    ↓
Parameter Extraction → {
    goal: "накопление",
    amount: 50000,
    timeframe: "среднесрочный",
    problem: "доставка"
}
    ↓
Comprehensive Analysis → {
    analysis: "Детальный анализ ситуации...",
    recommendations: [...],
    financial_plan: {...}
}
```

### Пример 3: Прогноз
```
Пользователь: "Что будет, если уменьшу траты на развлечения на 25%?"
    ↓
Forecast Generation → {
    category: "Entertainment",
    current_monthly: 10000,
    change_percent: -25,
    new_monthly: 7500,
    monthly_forecast: [...],
    total_savings: 7500
}
```

---

*Документ создан на основе анализа кодовой базы AI Finance Assistant*

