# Быстрое тестирование голосового ввода

## Шаг 1: Запуск Backend

```bash
cd backend
python main.py
```

Проверьте, что сервер запустился:
```bash
curl http://localhost:8000/health
```

## Шаг 2: Тестирование через приложение

1. Запустите MAUI приложение
2. Откройте чат (кнопка "AI")
3. Нажмите на кнопку микрофона 🎤
4. Разрешите доступ к микрофону
5. Произнесите: "Купил хлеб за 50 рублей"
6. Остановите запись
7. Проверьте результат

## Шаг 3: Тестирование через API (без аудио файла)

Проверьте извлечение транзакций из текста:

```bash
curl -X POST "http://localhost:8000/extract-transactions" \
  -H "Content-Type: application/json" \
  -d '{"user_message": "Купил хлеб за 50 рублей и молоко за 80 рублей"}'
```

## Шаг 4: Проверка логов

Если что-то не работает, проверьте:
- Логи backend в консоли
- Логи приложения (если доступны)
- Ошибки сети в браузере DevTools (для веб-версии)

## Возможные проблемы

1. **Backend не запускается**: Проверьте зависимости `pip install -r requirements.txt`
2. **Микрофон не работает**: Проверьте разрешения в настройках устройства
3. **API недоступен**: Убедитесь, что backend запущен на порту 8000
4. **Распознавание не работает**: Проверьте доступность GigaChat API

